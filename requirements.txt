# ============================================================
# Intelligent Fitness Recommendation & Coaching System
# requirements.txt
# Python 3.10+
# ============================================================

# ── Data & Video Pipeline (Member 1) ──────────────────────
opencv-python==4.9.0.80          # Frame extraction từ video
google-api-python-client==2.120.0 # YouTube Data API v3
requests==2.31.0
beautifulsoup4==4.12.3

# ── Pose Recognition (Member 2) ───────────────────────────
mediapipe==0.10.11               # Pose extraction (33 keypoints)
numpy==1.26.4
torch==2.2.0                     # LSTM / GRU training
torchvision==0.17.0
scikit-learn==1.4.0              # MLP baseline, metrics

# ── Recommendation System (Member 3) ──────────────────────
pandas==2.2.1
scipy==1.12.0                    # Matrix factorization (SVD)
# torch đã cài ở trên

# ── RAG / NLP System (Member 4) ───────────────────────────
langchain==0.1.12
langchain-community==0.0.28
langchain-openai==0.0.8          # hoặc langchain-google-genai
chromadb==0.4.24                 # Vector store
sentence-transformers==2.6.1     # Embedding model
openai==1.14.0                   # LLM API (có thể thay bằng Gemini)

# ── Backend API (Member 5) ────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.28.0
python-jose[cryptography]==3.3.0 # JWT auth
passlib[bcrypt]==1.7.4
psycopg2-binary==2.9.9           # PostgreSQL driver
sqlalchemy==2.0.28
alembic==1.13.1                  # DB migrations
python-multipart==0.0.9          # File upload support
pydantic==2.6.3

# ── Frontend build tools (nếu dùng Vite/React, cài riêng bằng npm)

# ── Utilities ─────────────────────────────────────────────
python-dotenv==1.0.1             # Load .env file
httpx==0.27.0                    # Async HTTP client
tqdm==4.66.2                     # Progress bars

# ── Notebooks / Visualization ─────────────────────────────
jupyter==1.0.0
matplotlib==3.8.0
seaborn==0.13.2
plotly==5.20.0
